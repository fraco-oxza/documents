## Estudiar la teoría cromosomica de la herencia 

```rust
// src/main.rs
use std::thread;
use std::time::Duration;
enum Motors {
        Electric,
    }

fn main() {
    loop {
        println!("Hello World!");
        thread::sleep(Duration::from_secs(2));
    }
}
```
